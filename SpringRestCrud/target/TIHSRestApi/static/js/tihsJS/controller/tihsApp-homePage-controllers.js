'use strict';

/* Controllers */


/*
 * default home controller is used to navigate the use to the default home page.
 * 
 * It will check the length of the available projects if any 
 * and redirect to the home page where all the available projects will be displayed
 * 
 */
tihsApp.controller('defaultHomePageController', function($scope,$location) {
	//$location.path("/homePage");
});
